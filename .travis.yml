language: r
sudo: false
cache: packages
pandoc: false
latex: false

install:
  - true

script:
  - echo $TRAVIS_PULL_REQUEST
  - echo $TRAVIS_BRANCH
  - git diff --name-only $TRAVIS_COMMIT_RANGE | grep -E 'staging\.txt' || echo test
  - Rscript R/create-md.R
  - git add -A
  - git diff --cached
